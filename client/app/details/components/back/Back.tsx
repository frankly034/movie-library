import styled from "styled-components";

import { CustomLink } from "../../../../components";

const Back = styled(CustomLink)`
  font-size: 48px;
  margin-bottom: 24px;
  color: #ffbf00;
`;

export default Back;
